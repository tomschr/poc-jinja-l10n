<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <title>{{ 'hello' | translate(locale) }}</title>
</head>
<body>
    <p>{{ 'hello' | translate(locale) }}</p>
    <p>{{ 'goodbye' | translate(locale) }}</p>
</body>
</html>
